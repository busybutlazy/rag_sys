# ── LLM ──────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── Auth ─────────────────────────────────────────
# Single-user credentials (Phase 1 will read these to seed the DB)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme_strong_password

# JWT signing secret (min 32 chars)
JWT_SECRET=changeme_32_char_minimum_secret_here

# ── MySQL ─────────────────────────────────────────
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=rag_sys
MYSQL_USER=raguser
MYSQL_PASSWORD=ragpass

# ── ArangoDB ─────────────────────────────────────
ARANGO_ROOT_PASSWORD=arangopass
ARANGO_DB=rag_db
ARANGO_USER=raguser
ARANGO_PASSWORD=ragpass

# Shared secret for internal service-to-service calls (be-server → rag-server)
INTERNAL_SECRET=changeme_internal_secret_here_32_chars
RAG_INTERNAL_SECRET=changeme_rag_internal_secret_here_32_chars
AI_INTERNAL_SECRET=changeme_ai_internal_secret_here_32_chars

# ── Services (internal — normally not changed) ────
BE_SERVER_PORT=8001
AI_SERVER_PORT=8002
RAG_SERVER_PORT=8003
FRONTEND_PORT=5987
